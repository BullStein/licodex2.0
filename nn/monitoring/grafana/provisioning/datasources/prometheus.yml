# Auto-provisiona o Prometheus como datasource padrão do Grafana, pra
# você não ter que configurar nada manualmente na primeira vez que abrir.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
